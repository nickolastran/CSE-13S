#include <math.h>
#include <stdio.h>

int main(void) {
    printf(" 1.0 / 0.0 is %f\n", 1.0 / 0.0);
    printf("-1.0 / 0.0 is %f\n", -1.0 / 0.0);
    printf("sqrt(-1.0) is %f\n", sqrt(-1.0));
    printf(" 0.0 / 0.0 is %f\n", 0.0 / 0.0);
    return 0;
}
